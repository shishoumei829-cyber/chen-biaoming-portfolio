---
description: 全项目通用：以用户感知为唯一标准，禁止补丁式堆叠与未验证的「已修复」
alwaysApply: true
---

# 代理工作宪法（全对话生效）

## 根本原则

1. **用户眼睛看到的 = 唯一真相**；代码能跑 ≠ 任务完成。
2. **先定体验宪法，再写代码**；不得用参数调优代替架构/叙事错误。
3. **每一轮只维护一条产品路径**；新方案必须删除旧方案，禁止 burst + pin + hold + bridge 叠在一起。
4. **完成感不能代替正确性**；未验证前禁止说「已修好」「请试一下」。

## 动手前（强制）

1. **读当前文件**，不信会话摘要里的版本号/结构（先 `Read` 再改）。
2. 用用户语言写清 **禁止出现** 与 **必须出现**（各 3～7 条），再改代码。
3. 方向含糊时 **先问一句**（例如：「一屏」指哪块 DOM、轻滑阈值），不要猜着做特效。

## 动手中（强制）

1. **最小 diff**：只改完成任务所需的文件；不顺手重构无关模块。
2. **滚动只认一套策略**：Lenis 与 ScrollTrigger snap 不能双头吸附；`preventDefault` 与 `scrollTo` 必须有明确 handoff。
3. **禁止用通用「高级滚动 demo」替代用户叙事**（氛围图、空 pin 红场、多出一页过渡层等）。

## 声称完成前（强制）

1. 对照体验宪法逐条自检；数字方舟见 `digitalark-scroll.mdc` 四步验收。
2. 动效相关：确认缓存版本已 bump，并提醒用户硬刷新。
3. 用户纠正过的问题：写入 `.cursor/rules/` 或 `AGENTS.md`，避免下一会话重犯。

## 用户纠正时的默认动作

- 不是再加一层动效，而是 **找出哪条旧逻辑还在跑并删掉**。
- 用中文简短说明：**删了什么 / 为什么之前会错 / 现在怎么验收**。
